hmapp.controller('shortlistcandidateController', shortlistcandidateController);

shortlistcandidateController.$inject = ['$rootScope', '$scope', '$state', 'ApiService', '$window', '$timeout', '$interval'];

function shortlistcandidateController($rootScope, $scope, $state, ApiService, $window, $timeout, $interval) {
	$scope.pageInfo = {};
}